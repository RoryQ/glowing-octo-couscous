name: required
on:
  pull_request:

jobs:
  line-coverage:
    runs-on: ubuntu-latest
    steps:
      - run: echo "success"
        
  required-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "success"
